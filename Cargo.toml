[workspace]
members = ["daedalus-core", "daedalus-stub", "daedalus-cli"]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
overflow-checks = true
