name: Test daedalus Action

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/actions/daedalus-build/**'

jobs:
  test-action:
    name: Test daedalus-build action
    runs-on: ubuntu-latest
    env:
      CARGO_TARGET_DIR: /tmp/daedalus-stub-target
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools zstd pkg-config libssl-dev python3

      - name: Build stub (musl static ELF)
        run: make stub

      # Build and install daedalus so the action can find it
      - name: Build daedalus from source
        run: |
          cargo build --release -p daedalus-cli
          /tmp/daedalus-stub-target/release/daedalus --version

      # Create a minimal test app
      - name: Create test app
        run: |
          mkdir -p /tmp/test-app
          cat > /tmp/test-app/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head><title>Test</title></head>
          <body><h1>Hello from daedalus action!</h1></body>
          </html>
          EOF

      # Test the action
      - name: Build test binary with action
        uses: ./.github/actions/daedalus-build
        id: daedalus
        with:
          app-path: '/tmp/test-app'
          output: '/tmp/test-app.ere'

      # Verify output
      - name: Verify output
        run: |
          echo "Binary path: ${{ steps.daedalus.outputs.binary-path }}"
          echo "Runtime: ${{ steps.daedalus.outputs.runtime }}"
          echo "Size: ${{ steps.daedalus.outputs.size }}"

          test -f "${{ steps.daedalus.outputs.binary-path }}" || {
            echo "ERROR: binary not found"
            exit 1
          }

          echo "PASS: binary exists"

      # Upload artifact
      - uses: actions/upload-artifact@v6
        with:
          name: test-binary
          path: /tmp/test-app.ere
